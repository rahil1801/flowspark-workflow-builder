## Packages
(none needed)

## Notes
Uses existing shadcn/ui components in client/src/components/ui
All fetches use credentials: "include"
Zod validation uses schemas from @shared/routes; date fields are coerced on the client
No stock images
